import React, {useContext} from "react";
import {PaginasContainer} from "../../PaginasContainer";

export const Painel = () => {

    return (
        <PaginasContainer>
            <h1>Painel - Painel</h1>
        </PaginasContainer>

    );
}